// Solution · BlogPost · TeamMember types — populated in Session 3
export type Solution = {
  slug: string
  title: string
}

export type BlogPost = {
  slug: string
  title: string
}

export type TeamMember = {
  name: string
  role: string
}
